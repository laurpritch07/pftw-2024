function fetchData() {
    // fetch here
}
function isDataCorrect() {
    // check to see if data is expected
}

function showGrowler() {
    // present feedback to user
}

window.addEventListener("load", function() {
    const response = fetchData();
    if(isDataCorrect(response))
}
)